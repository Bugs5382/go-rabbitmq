# local-only, never committed
plan/
.claude/

# Go
/bin/
*.test
*.out
coverage.out

# node tooling (husky/commitlint)
node_modules/

# OS / editor
.DS_Store
.idea/
.vscode/

# Track the shared governance (hooks + settings); other .claude stays local.
!/.claude/
/.claude/*
!/.claude/hooks/
!/.claude/settings.json
